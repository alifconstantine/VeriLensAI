import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Forced reload to clear Turbopack cache for middleware rename
  // Forced reload for .env.local update
};

export default nextConfig;
